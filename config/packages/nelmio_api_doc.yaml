nelmio_api_doc:
  documentation:
    info:
      title: Book&Go Bot
      version: 1.0.0
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT
          description: "Enter JWT access token"
  areas:
    default:
      path_patterns:
        - ^/api/v1(?!/telegram|/docs)
